# Cases that must stay bit-identical across macOS / Linux / Windows in the CONTROLLED build
# (config B: MR_ADM_STRICT_FP=ON, MR_ADM_EAR_SCALAR_REFERENCE=ON).
#
# Separate from expected-identical.txt on purpose. Config B is expected to converge on cases the
# default build does not, and gating the default build on those would make it permanently red
# for a result it never claimed. Config A keeps its own list.
#
# Empty on purpose for the same reason as the default list: nothing has been shown to hold
# across all three platforms yet. Populate from the first full three-platform run.
#
# One case name per line; '#' starts a comment.
